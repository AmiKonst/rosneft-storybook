const __resolved__virtual_storySource_srcStoriesAssetsTokenStoryVue = "<template>\r\n    <Story title=\"Assets/Цветовые токены\">\r\n        <LocalSettings />\r\n\r\n        <Variant title=\"Токены\">\r\n            <div v-for=\"(tokens, group) in groupedColorTokens\" :key=\"group\" class=\"group\">\r\n                <h3 class=\"group-title\">{{ group }}</h3>\r\n                <div class=\"grid\">\r\n                    <div\r\n                        class=\"token\"\r\n                        v-for=\"(value, name) in tokens\"\r\n                        :key=\"name\"\r\n                    >\r\n                        <div\r\n                            class=\"swatch\"\r\n                            :style=\"{ backgroundColor: `var(${name})` }\"\r\n                        ></div>\r\n                        <div class=\"label\">\r\n                            <code>{{ name }}</code>\r\n                            <span>{{ value }}</span>\r\n                        </div>\r\n                    </div>\r\n                </div>\r\n            </div>\r\n        </Variant>\r\n    </Story>\r\n</template>\r\n\r\n<script setup>\r\nimport { ref, onMounted } from 'vue'\r\nimport LocalSettings from '../LocalSettings.vue'\r\n\r\nconst groupedColorTokens = ref({});\r\n\r\nconst collectAllCSSVariables = () => {\r\n    const vars = {}\r\n    for (const sheet of document.styleSheets) {\r\n        try {\r\n            for (const rule of sheet.cssRules) {\r\n                if (rule.selectorText === ':root') {\r\n                    const style = rule.style\r\n                    for (let i = 0; i < style.length; i++) {\r\n                        const name = style[i]\r\n                        if (\r\n                            name.startsWith('--') &&\r\n                            !['--color-scheme', 'filter', 'font'].some(item =>\r\n                                name.toLowerCase().includes(item)\r\n                            )\r\n                        ) {\r\n                            vars[name] = style.getPropertyValue(name).trim()\r\n                        }\r\n                    }\r\n                }\r\n            }\r\n        } catch (e) {\r\n            // Игнорировать CORS-проблемные стили\r\n        }\r\n    }\r\n    return vars\r\n}\r\n\r\nconst groupTokens = (tokens) => {\r\n    const grouped = {};\r\n    for (const [name, value] of Object.entries(tokens)) {\r\n        // Получаем первую часть токена (после -- и до первого \"-\")\r\n        const key = name.replace(/^--/, '').split('-')[0]\r\n        const groupName = key.charAt(0).toUpperCase() + key.slice(1)\r\n        if (!grouped[groupName]) {\r\n            grouped[groupName] = {}\r\n        }\r\n        grouped[groupName][name] = value\r\n    }\r\n    return grouped\r\n}\r\n\r\nonMounted(() => {\r\n    const tokens = collectAllCSSVariables();\r\n    groupedColorTokens.value = groupTokens(tokens);\r\n});\r\n</script>\r\n\r\n<style lang=\"scss\" scoped>\r\n.grid {\r\n    display: grid;\r\n    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));\r\n    gap: 16px;\r\n}\r\n\r\n.group {\r\n    margin-bottom: 32px;\r\n}\r\n\r\n.group-title {\r\n    font-size: 18px;\r\n    font-weight: bold;\r\n    margin-bottom: 12px;\r\n    color: var(--black);\r\n}\r\n\r\n.token {\r\n    display: flex;\r\n    align-items: center;\r\n    gap: 12px;\r\n    padding: 8px;\r\n    border: 1px solid var(--black-100);\r\n    border-radius: 8px;\r\n    background: var(--white);\r\n}\r\n\r\n.swatch {\r\n    width: 32px;\r\n    height: 32px;\r\n    border-radius: 4px;\r\n    // border: 1px solid #ccc;\r\n}\r\n\r\n.label {\r\n    display: flex;\r\n    flex-direction: column;\r\n    font-size: 14px;\r\n\r\n    code {\r\n        font-weight: bold;\r\n    }\r\n\r\n    span {\r\n        font-size: 12px;\r\n        color: #666;\r\n    }\r\n}\r\n</style>\r\n";

export { __resolved__virtual_storySource_srcStoriesAssetsTokenStoryVue as default };
